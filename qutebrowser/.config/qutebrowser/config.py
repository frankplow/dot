import os

# Autogenerated config.py
# Documentation:
#   qute://help/configuring.html
#   qute://help/settings.html

config.bind(',p', ':spawn --userscript qute-pass --username-target secret --username-pattern "UserName: (.+)"')
config.bind(',P', ':spawn --userscript qute-pass --username-target secret --username-pattern "UserName: (.+)" --password-only')

# Uncomment this to still load settings configured via autoconfig.yml
# config.load_autoconfig()

# Additional arguments to pass to Qt, without leading `--`. With
# QtWebEngine, some Chromium arguments (see
# https://peter.sh/experiments/chromium-command-line-switches/ for a
# list) will work.
# Type: List of String
c.qt.args = ['ppapi-widevine-path=/usr/lib/qt/plugins/ppapi/libwidevinecdmadapter.so']

# List of URLs of lists which contain hosts to block.  The file can be
# in one of the following formats:  - An `/etc/hosts`-like file - One
# host per line - A zip-file of any of the above, with either only one
# file, or a file   named `hosts` (with any extension).  It's also
# possible to add a local file or directory via a `file://` URL. In case
# of a directory, all files in the directory are read as adblock lists.
# The file `~/.config/qutebrowser/blocked-hosts` is always read if it
# exists.
# Type: List of Url
c.content.host_blocking.lists = ['https://raw.githubusercontent.com/StevenBlack/hosts/master/hosts']

# A list of patterns that should always be loaded, despite being ad-
# blocked. Note this whitelists blocked hosts, not first-party URLs. As
# an example, if `example.org` loads an ad from `ads.example.org`, the
# whitelisted host should be `ads.example.org`. If you want to disable
# the adblocker on a given page, use the `content.host_blocking.enabled`
# setting with a URL pattern instead. Local domains are always exempt
# from hostblocking.
# Type: List of UrlPattern
c.content.host_blocking.whitelist = ['piwik.org', 'adf.ly']

# Enable JavaScript.
# Type: Bool
config.set('content.javascript.enabled', True, 'file://*')

# Enable JavaScript.
# Type: Bool
config.set('content.javascript.enabled', True, 'chrome://*/*')

# Enable JavaScript.
# Type: Bool
config.set('content.javascript.enabled', True, 'qute://*/*')

# Directory to save downloads to. If unset, a sensible OS-specific
# default is used.
# Type: Directory
c.downloads.location.directory = '$HOME/downloads'

# Page to open if :open -t/-b/-w is used without URL. Use `about:blank`
# for a blank page.
# Type: FuzzyUrl
c.url.default_page = 'https://www.google.co.uk'

# Open base URL of the searchengine if a searchengine shortcut is
# invoked without parameters.
# Type: Bool
c.url.open_base_url = False

# Search engines which can be used via the address bar. Maps a search
# engine name (such as `DEFAULT`, or `ddg`) to a URL with a `{}`
# placeholder. The placeholder will be replaced by the search term, use
# `{{` and `}}` for literal `{`/`}` signs. The search engine named
# `DEFAULT` is used when `url.auto_search` is turned on and something
# else than a URL was entered to be opened. Other search engines can be
# used by prepending the search engine name to the search term, e.g.
# `:open google qutebrowser`.
# Type: Dict
c.url.searchengines = {'DEFAULT': 'https://www.google.com/search?q={}'}

# Page(s) to open at the start.
# Type: List of FuzzyUrl, or FuzzyUrl
c.url.start_pages = 'https://www.google.co.uk'

# URL parameters to strip with `:yank url`.
# Type: List of String
c.url.yank_ignored_parameters = ['ref', 'utm_source', 'utm_medium', 'utm_campaign', 'utm_term', 'utm_content']

c.input.mouse.back_forward_buttons = False

#
# Colors
#

c.colors.completion.category.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.completion.category.fg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.completion.even.bg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.completion.odd.bg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.completion.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.completion.item.selected.bg = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.completion.item.selected.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.completion.item.selected.border.bottom = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.completion.item.selected.border.top = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.completion.match.fg = '#'+os.environ["THEME_COLOUR12"]
c.colors.completion.item.selected.match.fg = '#'+os.environ["THEME_COLOUR12"]

c.colors.statusbar.normal.fg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.statusbar.normal.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.statusbar.insert.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.statusbar.insert.bg = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.statusbar.passthrough.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.statusbar.passthrough.bg = '#'+os.environ["THEME_COLOUR14"]
c.colors.statusbar.private.fg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.statusbar.private.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.statusbar.command.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.statusbar.command.bg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.statusbar.command.private.fg = '#'+os.environ["THEME_FOREGROUND"]
c.colors.statusbar.command.private.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.statusbar.progress.bg = '#'+os.environ["THEME_COLOUR6"]
c.colors.statusbar.url.fg = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.statusbar.url.error.fg = '#'+os.environ["THEME_COLOUR9"]
c.colors.statusbar.url.hover.fg = '#'+os.environ["THEME_COLOUR14"]
c.colors.statusbar.url.success.http.fg = '#'+os.environ["THEME_COLOUR10"]
c.colors.statusbar.url.warn.fg = '#'+os.environ["THEME_COLOUR1"]

# Color gradient interpolation system for the tab indicator.
# Type: ColorSystem
# Valid values:
#   - rgb: Interpolate in the RGB color system.
#   - hsv: Interpolate in the HSV color system.
#   - hsl: Interpolate in the HSL color system.
#   - none: Don't show a gradient.
c.colors.tabs.indicator.system = 'rgb'

c.colors.tabs.bar.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.tabs.odd.fg = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.tabs.odd.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.tabs.even.fg = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.tabs.even.bg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.tabs.selected.odd.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.tabs.selected.odd.bg = '#'+os.environ["THEME_HIGHLIGHT"]
c.colors.tabs.selected.even.fg = '#'+os.environ["THEME_BACKGROUND"]
c.colors.tabs.selected.even.bg = '#'+os.environ["THEME_HIGHLIGHT"]

# Default monospace fonts. Whenever "monospace" is used in a font
# setting, it's replaced with the fonts listed here.
# Type: Font
c.fonts.default_family = [os.environ["THEME_FONT_TEXT"], "xos4 Terminus", "Terminus", "Monospace", "DejaVu Sans Mono", "Monaco", "Bitstream Vera Sans Mono", "Andale Mono", "Courier New", "Courier", "Liberation Mono", "monospace", "Fixed", "Consolas", "Terminal"]
c.fonts.completion.entry = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.completion.category = 'bold '+os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.debug_console = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.downloads = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.hints = 'bold '+os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.keyhint = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.messages.error = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.messages.info = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.messages.warning = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.prompts = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.tabs.selected = os.environ["THEME_FONT_SIZE"]+'pt default_family'
c.fonts.tabs.unselected = os.environ["THEME_FONT_SIZE"]+'pt default_family'

# Hard minimum font size (in pixels).
# Type: Int
c.fonts.web.size.minimum = 0

# Minimum logical font size (in pixels) that is applied when zooming
# out.
# Type: Int
c.fonts.web.size.minimum_logical = 6
